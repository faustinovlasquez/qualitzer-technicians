import { Text, View } from "react-native";
import type { GoogleMapProps } from "./googleMapProtocol";

export function GoogleMap(_props: GoogleMapProps) {
  return <View><Text>El mapa nativo de Google está disponible en la aplicación Android.</Text></View>;
}